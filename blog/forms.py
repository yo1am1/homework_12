from blog.models import Publication

from django import forms


# class PublicationForm(forms.Form):
#     id = forms.IntegerField(label="Find by ID", min_value=1, max_value=Publication.objects.count())
